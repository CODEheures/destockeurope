<div class="ui grid">
    <div class="one column computer only row">
        <a class="logo item ripple-me" href="{{ route('portal') }}">
            <img class="ui image" src="{{ asset('images/logo.svg') }}">
        </a>
    </div>
    <div class="one column tablet only row">
        <a class="logo item ripple-me" href="{{ route('portal') }}">
            <img class="ui small image" src="{{ asset('images/logo.svg') }}">
        </a>
    </div>
    <div class="one column mobile only row">
        <a class="logo item ripple-me" href="{{ route('portal') }}">
            <img class="ui tiny image" src="{{ asset('images/logo.svg') }}">
        </a>
    </div>
</div>